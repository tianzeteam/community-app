#开发环境配置
myprofile.name=dev
#日志配置
logging.config=classpath:log4j2-spring-dev.xml
#自定已日志级别
#logging.level.
#datasource
spring.datasource.url = jdbc:postgresql://${mysql.host:localhost}:5432/${mysql.dbname:test}
spring.datasource.username = ${mysql.username:postgres}
spring.datasource.password = ${mysql.password:storm302}
#es
spring.elasticsearch.rest.uris=http://localhost:9200
spring.elasticsearch.rest.username=
spring.elasticsearch.rest.password=

#pagehelper
#默认false 设置为true时，会将RowBounds第一个参数offset当成pageNum页码使用 和startPage中的pageNum效果一样
pagehelper.offsetAsPageNum = true
#默认false，true会进行count查询
pagehelper.rowBoundsWithCount = true
#设置为true时，如果pageSize=0或者RowBounds.limit = 0就会查询出全部的结果
pagehelper.pageSizeZero = true
#启用合理化时，如果pageNum<1会查询第一页，如果pageNum>pages会查询最后一页 禁用合理化时，如果pageNum<1或pageNum>pages会返回空数据
pagehelper.reasonable = false
#支持通过Mapper接口参数来传递分页参数
pagehelper.supportMethodsArguments = false